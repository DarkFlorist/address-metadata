
export type TokenMetadataData = {
	address: string;
	data: {
		name: string;
		symbol: string;
		decimals: number;
		logoUri?: string;
	}
}
export const tokenMetadataData: Array<TokenMetadataData> = [
];
